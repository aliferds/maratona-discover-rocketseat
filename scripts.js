const Modal = {
    open(){
        document.querySelector(".modal-overlay").classList.toggle("active")
    },
    close(){
        document.querySelector(".modal-overlay").classList.toggle("active")
    }
}